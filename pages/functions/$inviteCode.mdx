import Callout from 'nextra-theme-docs/callout'

# $inviteCode

Returns the group's invitation code.

### Usage:

```plain
$inviteCode

// or with provided jid
$inviteCode[group id]
```

<Callout>
  The bot must be an admin in that group.
</Callout>
