# $image

Send a image message.

### Usage:

```
$image[image url]
```

### Example:

```javascript
bot.command({
    name: "image",
    code: `Text is required!
    $image[https://via.placeholder.com/150.jpeg]`
});
```
